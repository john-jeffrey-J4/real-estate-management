frontend:
-----------

1) all control type in one file has to change
2) uuid generating in frontend now 
3) in structure api-> custom_params datattpe change to json now in list
4) theme integration in dynamic form
5) clear all button and functionality in dynamic form
6) botttomsheet functionalities change 
-> what if we touch somewhere ekse in the screen it doesnt go away 
-> async function so when we click a value in bottomsheet only last the data





Backend:
---------


booking_date: Optional[dt] = None, 



if booking_date:
            query = query.filter(Booking.booking_date == booking_date)




K@71#1O0pj